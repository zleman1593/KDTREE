PLATFORM = $(shell uname)


## Compilation flags
##comment out one or the other 
##debugging 
CFLAGS = -g 
##release
#CFLAGS = -O3 -DNDEBUG
LDFLAGS=

CFLAGS+= -Wall

ifeq ($(PLATFORM),Darwin)
## Mac OS X
CFLAGS += -m64 -isystem/usr/local/include
LDFLAGS+= -m64 -lc -framework AGL -framework OpenGL -framework GLUT -framework Foundation
else
## Linux
CFLAGS += -m64
INCLUDEPATH  = -I/usr/include/GL/ 
LIBPATH = -L/usr/lib64 -L/usr/X11R6/lib
LDFLAGS+=  -lGL -lglut -lrt -lGLU -lX11 -lm  -lXmu -lXext -lXi
endif


CC = g++ $(CFLAGS) $(INCLUDEPATH)


PROGS = viewPoints

default: $(PROGS)

viewPoints: viewPoints.o rtimer.o  kdtree.o
	$(CC) -o $@ viewPoints.o rtimer.o  kdtree.o $(LDFLAGS)

viewPoints.o: viewPoints.cpp kdtree.h rtimer.h
	$(CC) -c $(INCLUDEPATH)  viewPoints.cpp  -o $@

rtimer.o: rtimer.h rtimer.cpp
	$(CC) -c $(INCLUDEPATH) rtimer.cpp -o $@

kdtree.o: kdtree.cpp kdtree.h geom.h
	$(CC) -c $(INCLUDEPATH)  kdtree.cpp -o $@

clean::	
	rm *.o
	rm viewPoints


